id: traffic.simulation
version: 1.0

inputs:
  add_vehicle:
    parameters:
      id: string
      route: string

  change_speed:
    parameters:
      id: string
      speed: float

  get_vehicle_state:
    parameters:
      id: string
    returns:
      speed: float
